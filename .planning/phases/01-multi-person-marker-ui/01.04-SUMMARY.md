# Phase 01: Multi-Person Marker UI - Gap Closure 2
**Status**: Complete

## Work Done
Addressed the missing image upload trigger during `isMultiPersonMode`:
- Added a floating "Change Image" (`Đổi ảnh`) button overlaid on the upper right of the single image preview.
- Wired this button to a hidden `input type="file"`.
- Implemented `handleDirectSubjectChange` to leverage `compressImage` utility for parity with what `MultiImageUploader` does normally.
- This restores the user's ability to seamlessly change their target image without manually clearing the current image or resetting the mode.

## Unplanned Work
None

## Next Phase Prep
Ready to advance to Verification re-test.
