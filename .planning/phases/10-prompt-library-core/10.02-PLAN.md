---
wave: 2
depends_on: ["10.01-PLAN.md"]
files_modified: ["src/components/PromptLibraryFAB.tsx", "src/components/modals/PromptLibraryModal.tsx", "src/App.tsx", "src/locales/en.ts", "src/locales/vi.ts"]
autonomous: true
requirements_addressed: ["PLIB-01", "PLIB-02", "PLIB-03"]
---

# Plan 10.02: Prompt Library UI and FAB

<objective>
Build the Prompt Library Modal, Search bar, and Floating Action Button (FAB).
</objective>

<must_haves>
- A new PromptLibraryModal component matching design system.
- A FAB in the bottom right corner injecting the modal (above the existing Gallery button).
- Delete button uses browser confirm().
- Search functionality inside modal.
</must_haves>

<tasks>
<task>
<description>Add localization strings</description>
<read_first>
- src/locales/en.ts
- src/locales/vi.ts
</read_first>
<action>
Add strings to `src/locales/en.ts` and `src/locales/vi.ts` inside a new `promptLibrary` object.
Include: `title: "Prompt Library"`, `searchPlaceholder: "Search prompts..."`, `emptyState: "No prompts found"`, `emptyStateBody: "You haven't saved any prompts yet."`, `curatedBadge: "Curated"`, `deleteConfirm: "Are you sure you want to delete this prompt?"`, `toastSaved: "Prompt saved"`.
</action>
<acceptance_criteria>
- `src/locales/en.ts` contains `promptLibrary:` and matching keys
- `src/locales/vi.ts` contains `promptLibrary:` and translated keys
</acceptance_criteria>
</task>

<task>
<description>Create Prompt Library Modal Component</description>
<read_first>
- src/components/modals/PromptLibraryModal.tsx
- src/hooks/usePromptLibrary.ts
- src/components/modals/GalleryModal.tsx (reference for modal wrapper)
</read_first>
<action>
Create `src/components/modals/PromptLibraryModal.tsx` taking `isOpen` and `onClose` props.
Uses `usePromptLibrary` hook.
Includes a text input for search, updating a local `searchQuery` state.
Displays `searchPrompts(searchQuery)` in a list.
Each item shows text, "Curated" badge if true, and a trash icon to delete if not curated. Uses `confirm(t("promptLibrary.deleteConfirm"))` on delete.
If empty, show empty state strings.
When an item is clicked, console.log(item.text) for now (to be wired fully in Phase 11).
</action>
<acceptance_criteria>
- `src/components/modals/PromptLibraryModal.tsx` exists and exports component
- Text input for search is present
- List of prompts rendered via `usePromptLibrary`
- Uses native `confirm` window dialog for deletions
</acceptance_criteria>
</task>

<task>
<description>Create Floating Action Button (FAB)</description>
<read_first>
- src/components/PromptLibraryFAB.tsx
- src/App.tsx
</read_first>
<action>
Create `src/components/PromptLibraryFAB.tsx`. 
Renders a fixed bottom-right button with an icon (e.g. BookmarkIcon or BookOpenIcon). Position should be `bottom-20 right-4` or similar to sit above `GalleryButton`.
Add `<PromptLibraryFAB onClick={() => setIsPromptLibraryOpen(true)} />` to `src/App.tsx`.
Manage `isPromptLibraryOpen` state in `App.tsx` and render `<PromptLibraryModal isOpen={isPromptLibraryOpen} onClose={() => setIsPromptLibraryOpen(false)} />` alongside other modals.
</action>
<acceptance_criteria>
- `PromptLibraryFAB.tsx` uses `fixed bottom-20 right-4` class logic
- `src/App.tsx` imports and renders `<PromptLibraryFAB />`
- `src/App.tsx` manages `isPromptLibraryOpen` state and renders `PromptLibraryModal`
</acceptance_criteria>
</task>
</tasks>

<verification>
grep "PromptLibraryModal" src/App.tsx
grep "PromptLibraryFAB" src/App.tsx
grep "promptLibrary" src/locales/en.ts
grep "searchQuery" src/components/modals/PromptLibraryModal.tsx
</verification>
