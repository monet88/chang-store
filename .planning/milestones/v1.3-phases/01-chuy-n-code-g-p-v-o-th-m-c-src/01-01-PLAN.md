---
phase: 01-chuy-n-code-g-p-v-o-th-m-c-src
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - tsconfig.json
  - vite.config.ts
  - vitest.config.ts
  - eslint.config.js
  - setupTests.ts
  - src/index.css
  - src/index.tsx
  - src/App.tsx
  - src/Spinner.tsx
  - src/types.ts
  - src/components/**
  - src/contexts/**
  - src/hooks/**
  - src/services/**
  - src/utils/**
  - src/locales/**
  - src/config/**
  - src/**/AGENTS.md
  - __tests__/**
  - AGENTS.md
  - CLAUDE.md
  - README.md
  - docs/codebase-summary.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "src/ is the only runtime source root — no duplicate root-level source directories remain"
    - "@/* resolves to src/ in TypeScript, Vite, and Vitest consistently"
    - "Repo-root tests import the moved runtime tree successfully"
    - "Browser bootstrap points at src/index.tsx and the app renders"
    - "Provider chain and feature routing behavior is unchanged"
    - "Docs and agent guidance describe the migrated layout accurately"
    - "npx tsc --noEmit, npm run lint, npm run test, and npm run build all pass"
  artifacts:
    - path: "src/index.tsx"
      provides: "Application bootstrap entry"
    - path: "src/App.tsx"
      provides: "App shell with provider chain and feature routing"
    - path: "src/types.ts"
      provides: "Shared domain types"
    - path: "src/components/"
      provides: "All UI components including directory-level AGENTS.md"
    - path: "src/contexts/"
      provides: "All context providers"
    - path: "src/hooks/"
      provides: "All feature hooks"
    - path: "src/services/"
      provides: "All service facades including gemini/ subdirectory"
    - path: "src/utils/"
      provides: "All utility modules"
    - path: "src/locales/"
      provides: "i18n translation files"
    - path: "src/config/"
      provides: "Config helpers including modelRegistry.ts"
  key_links:
    - from: "index.html"
      to: "src/index.tsx"
      via: "script module src attribute"
      pattern: 'src="/src/index.tsx"'
    - from: "tsconfig.json"
      to: "src/"
      via: "paths alias"
      pattern: '"@/*": ["./src/*"]'
    - from: "vite.config.ts"
      to: "src/"
      via: "resolve alias"
      pattern: "path.resolve(__dirname, 'src')"
    - from: "vitest.config.ts"
      to: "src/"
      via: "resolve alias and coverage include"
      pattern: "path.resolve(__dirname, 'src')"
    - from: "__tests__/**"
      to: "src/**"
      via: "import paths in tests and mocks"
---

<objective>
Move the Chang-Store runtime source tree into `src/` and retarget all path contracts, making `src/` the canonical and only application root.

Purpose: Consolidate runtime code under a standard `src/` directory so the project layout follows conventional SPA structure. Currently source code lives at repo root alongside config files, docs, and tests — causing confusion for both humans and AI agents about what is application code vs. project infrastructure.

Output: All runtime code under `src/`, all configs/tests/docs pointing at the new location, zero root-level runtime source remnants. Build, lint, type-check, and tests all green.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-chuy-n-code-g-p-v-o-th-m-c-src/01-CONTEXT.md
@.planning/phases/01-chuy-n-code-g-p-v-o-th-m-c-src/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate runtime source tree into src/</name>
  <files>
    src/index.tsx, src/App.tsx, src/types.ts, src/Spinner.tsx,
    src/components/**, src/contexts/**, src/hooks/**, src/services/**,
    src/utils/**, src/locales/**, src/config/**
  </files>
  <read_first>
    - App.tsx (current app shell — understand provider chain and feature routing)
    - index.tsx (current entry — understand bootstrap and CSS import)
    - types.ts (shared types — must move intact)
    - Spinner.tsx (note: this file is 0 bytes / empty — move it but flag if unused)
    - components/AGENTS.md (directory-level guidance to move with parent)
    - contexts/AGENTS.md
    - hooks/AGENTS.md
    - services/AGENTS.md
    - utils/AGENTS.md (if exists)
  </read_first>
  <action>
Move the runtime shell files and all technical-slice directories under `src/` in one pass, preserving the existing folder topology (per D-01, D-03, D-07).

**Concrete move scope:**

1. **Entry and shared files** — move into `src/`:
   - `App.tsx` → `src/App.tsx`
   - `index.tsx` → `src/index.tsx`
   - `types.ts` → `src/types.ts`
   - `Spinner.tsx` → `src/Spinner.tsx` (note: currently 0 bytes — move as-is)

2. **Technical-slice directories** — move entire directories under `src/`:
   - `components/` → `src/components/` (includes `components/AGENTS.md`, `components/shared/`, `components/modals/`, `components/upscale/`)
   - `contexts/` → `src/contexts/`
   - `hooks/` → `src/hooks/`
   - `services/` → `src/services/` (includes `services/gemini/` subdirectory)
   - `utils/` → `src/utils/`
   - `locales/` → `src/locales/`
   - `config/` → `src/config/`

3. **Keep `src/index.css` in place** — it already lives at `src/index.css`.

4. **DO NOT move** (stays at repo root):
   - `__tests__/` and `setupTests.ts` (per D-02)
   - `types/` directory (contains only `types/google.d.ts` — this is a TypeScript ambient declaration file, not runtime source)
   - All tooling configs: `tsconfig.json`, `vite.config.ts`, `vitest.config.ts`, `eslint.config.js`, `postcss.config.js`
   - `index.html`, `package.json`, and other project-level files
   - `docs/`, `.planning/`, `scripts/`, `plans/`

5. **Directory-level AGENTS.md files** move with their parent directory automatically.

6. **After the move, remove the now-empty root-level source directories** (per D-08): the old `components/`, `contexts/`, `hooks/`, `services/`, `utils/`, `locales/`, `config/` at repo root. Also remove `App.tsx`, `index.tsx`, `types.ts`, `Spinner.tsx` from repo root.

**Import adjustments inside moved files:**
- Relative imports between files that moved together (e.g., `./components/...` from `App.tsx`) should stay as relative imports since the topology is preserved (per D-05).
- The `import './src/index.css'` in `index.tsx` must become `import './index.css'` since `index.tsx` now lives alongside `index.css` in `src/`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx`, `src/index.tsx`, `src/types.ts`, and `src/Spinner.tsx` exist
    - `src/components/`, `src/contexts/`, `src/hooks/`, `src/services/`, `src/utils/`, `src/locales/`, and `src/config/` exist with all migrated files
    - No runtime source remains at root-level `components/`, `contexts/`, `hooks/`, `services/`, `utils/`, `locales/`, or `config/` directories
    - Root-level `App.tsx`, `index.tsx`, `types.ts`, `Spinner.tsx` no longer exist
    - `src/index.tsx` contains `import './index.css'` (not `import './src/index.css'`)
    - `src/App.tsx` still contains the same provider nesting: `LanguageProvider > ToastProvider > ApiProvider > GoogleDriveProvider > ImageGalleryProvider > ImageViewerProvider > AppContent`
    - `src/components/AGENTS.md`, `src/contexts/AGENTS.md`, `src/hooks/AGENTS.md`, `src/services/AGENTS.md` exist
  </acceptance_criteria>
  <done>All runtime source files live exclusively under src/ with preserved technical-slice topology. Root-level runtime copies removed. CSS import in index.tsx adjusted.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget alias, browser entry, build config, and all test imports</name>
  <files>
    index.html, tsconfig.json, vite.config.ts, vitest.config.ts,
    eslint.config.js, setupTests.ts, __tests__/**
  </files>
  <read_first>
    - index.html (line 236: currently `<script type="module" src="/index.tsx">` — must change to `/src/index.tsx`)
    - tsconfig.json (line 23-25: currently `"@/*": ["./*"]` — must change to `"@/*": ["./src/*"]`)
    - vite.config.ts (line 48: currently `'@': path.resolve(__dirname, '.')` — must change to `'src'`)
    - vitest.config.ts (lines 16, 35-40: alias and coverage.include paths)
    - eslint.config.js (check if any include patterns need updating)
    - __tests__/__mocks__/contexts.tsx (line 21: `import { Feature } from '../../types'` — must update to `../../src/types`)
    - __tests__/components/VirtualTryOn.test.tsx (sample test for import pattern)
    - __tests__/hooks/useUpscale.test.tsx (sample test — has many root-relative mocks)
    - __tests__/components/Upscale.test.tsx (sample test with root-relative mocks)
    - __tests__/services/upscaleAnalysisService.test.ts
  </read_first>
  <action>
Update every path contract that still assumes repo-root source (per D-04, D-05, D-06).

**1. Browser entry (`index.html`):**
- Change `<script type="module" src="/index.tsx"></script>` to `<script type="module" src="/src/index.tsx"></script>`

**2. TypeScript alias (`tsconfig.json`):**
- Change `"@/*": ["./*"]` to `"@/*": ["./src/*"]`

**3. Vite alias (`vite.config.ts`):**
- Change `'@': path.resolve(__dirname, '.')` to `'@': path.resolve(__dirname, 'src')`

**4. Vitest alias and coverage (`vitest.config.ts`):**
- Change `'@': path.resolve(__dirname, '.')` to `'@': path.resolve(__dirname, 'src')`
- Update coverage `include` paths from root-relative to `src/`-relative:
  - `'services/**/*.ts'` → `'src/services/**/*.ts'`
  - `'utils/**/*.ts'` → `'src/utils/**/*.ts'`
  - `'contexts/**/*.tsx'` → `'src/contexts/**/*.tsx'`
  - `'hooks/**/*.ts'` → `'src/hooks/**/*.ts'`
- Keep `setupFiles: './setupTests.ts'` unchanged (setupTests stays at root per D-02)

**5. ESLint config (`eslint.config.js`):**
- The `files: ['**/*.{ts,tsx,js,jsx}']` pattern is already broad enough to cover `src/`
- No changes needed unless specific include/exclude patterns reference root-level source dirs

**6. All 23 test files under `__tests__/` — rewrite root-relative imports:**
All `../../components/...`, `../../hooks/...`, `../../contexts/...`, `../../services/...`, `../../utils/...`, `../../locales/...`, `../../config/...` imports and `vi.mock()` paths must be updated to `../../src/components/...`, `../../src/hooks/...`, etc.

Concrete files that need import path rewrites (found by grep):
- `__tests__/__mocks__/contexts.tsx` — line 21: `import { Feature } from '../../types'` → `../../src/types`
- `__tests__/components/VirtualTryOn.test.tsx` — all mocks and imports targeting `../../hooks/`, `../../components/`, `../../contexts/`, `../../services/`, `../../utils/`
- `__tests__/components/ClothingTransfer.test.tsx` — same pattern
- `__tests__/components/Upscale.test.tsx` — same pattern
- `__tests__/hooks/useVirtualTryOn.test.tsx` — same pattern
- `__tests__/hooks/useClothingTransfer.test.tsx` — same pattern
- `__tests__/hooks/useUpscale.test.tsx` — same pattern
- `__tests__/hooks/useLookbookGenerator.test.tsx` — same pattern
- `__tests__/hooks/useOutfitAnalysis.test.tsx` — same pattern
- `__tests__/contexts/ApiProviderContext.test.tsx` — same pattern
- `__tests__/contexts/ImageGalleryContext.test.tsx` — same pattern
- `__tests__/contexts/LanguageContext.test.tsx` — same pattern
- `__tests__/contexts/GoogleDriveContext.test.tsx` — same pattern
- `__tests__/contexts/ImageViewerContext.test.tsx` — same pattern
- `__tests__/services/imageEditingService.test.ts` — same pattern
- `__tests__/services/apiClient.test.ts` — same pattern
- `__tests__/services/localProviderService.test.ts` — same pattern
- `__tests__/services/googleDriveService.test.ts` — same pattern
- `__tests__/services/upscaleAnalysisService.test.ts` — same pattern
- `__tests__/services/gemini/image.test.ts` — same pattern (note: three levels deep, so `../../../services/...` → `../../../src/services/...`)
- `__tests__/services/gemini/text.test.ts` — same pattern
- `__tests__/utils/imageUtils.test.ts` — same pattern
- `__tests__/utils/imageCache.test.ts` — same pattern
- `__tests__/utils/virtual-try-on-prompt-builder.test.ts` — same pattern

**Strategy:** For each test file, replace all `../../` paths targeting old root runtime dirs with `../../src/` equivalents. For files under `__tests__/services/gemini/`, the depth is `../../../` → `../../../src/`. Also check `vi.mock()` string arguments — they must match the same pattern update.

**Alternative approach if test files already use `@/` aliases:** Some mock paths in `vi.mock()` may use `@/contexts/...` style. After the alias retarget, these will resolve correctly without changes. But `vi.mock()` with relative paths like `../../hooks/useVirtualTryOn` MUST be updated.
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains `src="/src/index.tsx"` (not `src="/index.tsx"`)
    - `tsconfig.json` contains `"@/*": ["./src/*"]`
    - `vite.config.ts` contains `'@': path.resolve(__dirname, 'src')`
    - `vitest.config.ts` contains `'@': path.resolve(__dirname, 'src')`
    - `vitest.config.ts` coverage.include has `'src/services/**/*.ts'`, `'src/utils/**/*.ts'`, `'src/contexts/**/*.tsx'`, `'src/hooks/**/*.ts'`
    - No test file under `__tests__/` imports from `../../components/`, `../../hooks/`, `../../contexts/`, `../../services/`, `../../utils/`, `../../locales/`, `../../config/`, or `../../types` (without `src/` prefix)
    - `__tests__/__mocks__/contexts.tsx` imports from `../../src/types` (not `../../types`)
    - `npm run test -- VirtualTryOn ClothingTransfer useVirtualTryOn useClothingTransfer` passes
    - `npm run test` passes
  </acceptance_criteria>
  <done>All path contracts (browser entry, TS/Vite/Vitest aliases, coverage globs, and all 23 test file imports + mocks) point at the migrated src/ tree. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs and agent guidance to reflect migrated source root</name>
  <files>
    AGENTS.md, CLAUDE.md, README.md,
    docs/codebase-summary.md,
    src/components/AGENTS.md, src/contexts/AGENTS.md,
    src/hooks/AGENTS.md, src/services/AGENTS.md,
    __tests__/AGENTS.md,
    .planning/codebase/STRUCTURE.md, .planning/codebase/CONVENTIONS.md,
    .planning/codebase/STACK.md
  </files>
  <read_first>
    - AGENTS.md (path alias guidance, project structure table, directory notes)
    - CLAUDE.md (same content as AGENTS.md — they mirror each other)
    - README.md (Architecture section, directory structure)
    - docs/codebase-summary.md (directory structure diagram at lines 7-23 — all show root-level dirs)
    - src/components/AGENTS.md (may reference root-relative paths)
    - src/contexts/AGENTS.md
    - src/hooks/AGENTS.md
    - src/services/AGENTS.md
    - __tests__/AGENTS.md (test path conventions reference root-relative source)
    - .planning/codebase/STRUCTURE.md (line 84: says "src/ is not the main application root; it currently holds CSS only" — MUST be updated)
    - .planning/codebase/CONVENTIONS.md (references like `types.ts`, `components/LookbookGenerator.tsx` without `src/` prefix)
    - .planning/codebase/STACK.md (line 34: "Path alias @ points to repository root" — MUST be updated)
  </read_first>
  <action>
Update all docs and agent instructions so they describe the migrated tree accurately (per D-09).

**Top-level AGENTS.md and CLAUDE.md — both must have these changes:**
- Change `**Path alias**: \`@/*\` maps to project root (not \`src/\`)` → `**Path alias**: \`@/*\` maps to \`src/\``
- Update the Project Structure table: add `src/` prefix to directories: `src/components/`, `src/hooks/`, `src/services/`, `src/contexts/`, `src/utils/`, `src/locales/`, `src/config/`
- Change `> Source code lives at **project root** (not under \`src/\`). \`src/\` only contains \`index.css\`.` → `> Source code lives under \`src/\`. Entry files, components, hooks, services, contexts, utils, locales, and config all reside in \`src/\`.`
- Update all path examples to include `src/` prefix: `components/FeatureName.tsx` → `src/components/FeatureName.tsx`, `hooks/useFeatureName.ts` → `src/hooks/useFeatureName.ts`, etc.
- Update "Adding a new feature" steps: paths should reference `src/components/`, `src/hooks/`, `src/locales/`, `services/imageEditingService.ts` → `src/services/imageEditingService.ts`
- Update the Known Violations list paths to include `src/` prefix
- Update Anti-Patterns section paths
- Update the architecture provider nesting note: `components/Toast.tsx` → `src/components/Toast.tsx`
- Update the "Detailed Docs" section: `docs/design-guidelines.md` stays (it's in `docs/`, not moved), but any path references within should be checked

**README.md:**
- Update the Architecture section directory examples to use `src/` prefix
- Update the Provider Hierarchy note
- Update the Service Routing section: `imageEditingService.ts` → `src/services/imageEditingService.ts`

**docs/codebase-summary.md:**
- Update the directory structure diagram (lines 7-23) to show all runtime dirs under `src/`:
  ```
  Chang-Store/
  ├── src/                  # Application source root
  │   ├── components/       # React UI components
  │   ├── contexts/         # React Context providers
  │   ├── hooks/            # Custom hooks per feature
  │   ├── services/         # API service layer
  │   ├── locales/          # i18n translations
  │   ├── utils/            # Utility functions
  │   ├── config/           # Config helpers
  │   ├── App.tsx           # Root component
  │   ├── index.tsx         # Entry point
  │   ├── index.css         # Global styles
  │   └── types.ts          # Shared TypeScript definitions
  ├── __tests__/            # Test suites (intentionally at repo root)
  ...
  ```
- Update all file path references in the Key Files tables

**Directory-level AGENTS.md files (now under src/):**
- Verify internal path references are accurate after the move
- Confirm `components/Toast.tsx` reference is updated to `src/components/Toast.tsx` if referenced from root-level docs

**__tests__/AGENTS.md:**
- Update the structure listing if it references source directories without `src/` prefix
- Update any mock import path examples

**.planning/codebase/STRUCTURE.md — critical updates:**
- Remove line 84: `The \`src/\` directory is not the main application root; it currently holds CSS only.`
- Replace with: `The \`src/\` directory is the application root. All runtime code lives here.`
- Update all source directory references to include `src/` prefix
- Update the "Placement Rules for New Code" section to reference `src/` paths

**.planning/codebase/CONVENTIONS.md:**
- Update path examples: `types.ts` → `src/types.ts`, `components/LookbookGenerator.tsx` → `src/components/LookbookGenerator.tsx`, etc.

**.planning/codebase/STACK.md:**
- Change line 34: `Path alias @ points to repository root` → `Path alias @ points to src/ directory`
- Update lines 5 and 13 to show files under `src/`

**Focus only on stale path and source-root guidance per D-09. Do NOT rewrite doc content that is still accurate.**
  </action>
  <verify>
    <automated>grep -rn "Source code lives at.*project root\|src/.*only contains\|maps to project root\|maps to.*repository root\|@.*points to repository root\|src.*is not the main\|src.*holds CSS only" AGENTS.md CLAUDE.md README.md docs/codebase-summary.md .planning/codebase/STRUCTURE.md .planning/codebase/STACK.md .planning/codebase/CONVENTIONS.md 2>/dev/null | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` contains `@/*\` maps to \`src/\`` (not `maps to project root`)
    - `AGENTS.md` contains `Source code lives under \`src/\`` (not `Source code lives at **project root**`)
    - `CLAUDE.md` mirrors the same updates as `AGENTS.md`
    - `docs/codebase-summary.md` directory tree shows `src/` as parent of `components/`, `hooks/`, etc.
    - `.planning/codebase/STRUCTURE.md` does NOT contain "src/ directory is not the main application root" or "holds CSS only"
    - `.planning/codebase/STACK.md` contains "Path alias @ points to src/ directory" (not "repository root")
    - `__tests__/AGENTS.md` accurately describes test import patterns targeting `src/`
    - Grep for stale phrases `"Source code lives at.*project root"`, `"src.*only contains"`, `"maps to project root"`, `"src.*holds CSS only"` returns zero hits across all updated files
  </acceptance_criteria>
  <done>All docs and agent guidance accurately describe src/ as the application root. No stale root-source-path claims remain in AGENTS.md, CLAUDE.md, README.md, docs/codebase-summary.md, __tests__/AGENTS.md, or .planning/codebase/ files.</done>
</task>

<task type="auto">
  <name>Task 4: Run full validation loop and fix any regressions</name>
  <files>package.json, src/index.tsx, src/App.tsx</files>
  <read_first>
    - package.json (verify script commands still work with new layout)
    - .planning/phases/01-chuy-n-code-g-p-v-o-th-m-c-src/01-VALIDATION.md (validation strategy)
    - src/index.tsx (confirm CSS import and App import)
    - src/App.tsx (confirm provider chain intact)
  </read_first>
  <action>
Run the complete phase validation stack. Fix any regressions found. Do NOT skip failing checks.

**Validation sequence (run in this order):**

1. **TypeScript check:** `npx tsc --noEmit`
   - If fails: fix import paths in the reported files. Most likely causes: stale root-relative imports that weren't updated, or alias misconfigurations.

2. **Lint check:** `npm run lint`
   - If fails: fix lint errors. Most likely cause: new file patterns not matching ESLint config.

3. **Targeted critical tests:** `npm run test -- VirtualTryOn ClothingTransfer useVirtualTryOn useClothingTransfer`
   - These test the most complex feature hooks/components. If they pass, simpler tests likely pass too.
   - If fails: check mock paths and import rewrites in the failing test files.

4. **Full test suite:** `npm run test`
   - Must run all 23 test files. Check for failures in context tests, service tests, and util tests.
   - If fails: the most common cause is a `vi.mock()` path that still targets a root-level directory.

5. **Production build:** `npm run build`
   - Verifies Vite can resolve all imports and produce the dist/ output.
   - If fails: check alias config in vite.config.ts and any dynamic imports in App.tsx.

6. **Dev smoke test:** `npm run dev` — manually confirm:
   - App shell boots without console errors
   - Provider stack works (no "used outside provider" errors)
   - Feature switching renders at least 2-3 different features
   - The browser URL loads from /src/index.tsx (check the page source or network tab)

**For any failing step:** Fix the root cause in the offending file, then re-run from that step onward. Do NOT proceed to the next step until the current one passes.

**After all checks pass:** Verify no stale root-level source dirs remain:
```bash
ls -d components/ contexts/ hooks/ services/ utils/ locales/ config/ 2>/dev/null
```
This should produce no output (all moved to src/).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - `npm run lint` exits with code 0
    - `npm run test -- --run` exits with code 0 with all 23 test files passing
    - `npm run build` exits with code 0 and produces `dist/` output
    - `ls -d components/ contexts/ hooks/ services/ utils/ locales/ config/` at repo root returns empty / not found
    - Dev server boots and the app renders features without errors
  </acceptance_criteria>
  <done>Full validation loop passes: typecheck, lint, all 23 tests, and production build. No root-level runtime source directories remain. Dev smoke test confirms the app boots from src/.</done>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
# 1. Typecheck
npx tsc --noEmit

# 2. Lint
npm run lint

# 3. Critical tests
npm run test -- VirtualTryOn ClothingTransfer useVirtualTryOn useClothingTransfer

# 4. Full tests
npm run test

# 5. Production build
npm run build

# 6. Confirm no stale root source dirs
ls -d components/ contexts/ hooks/ services/ utils/ locales/ config/ 2>/dev/null && echo "STALE DIRS FOUND — FAIL" || echo "CLEAN — PASS"

# 7. Confirm no stale doc claims
grep -rn "Source code lives at.*project root\|src.*only contains\|maps to project root\|src.*holds CSS only" AGENTS.md CLAUDE.md README.md docs/codebase-summary.md .planning/codebase/ 2>/dev/null && echo "STALE DOCS — FAIL" || echo "DOCS CLEAN — PASS"
```
</verification>

<success_criteria>
1. Runtime source of truth lives under `src/` and no root-level runtime source directories remain active
2. `@` resolves to `src/` across TypeScript, Vite, and Vitest
3. All 23 repo-root tests import from the migrated `src/` tree and pass
4. Browser bootstrap loads `/src/index.tsx`
5. Provider chain and feature routing behavior unchanged
6. All docs and agent guidance accurately describe `src/` as the application root
7. `npx tsc --noEmit`, `npm run lint`, `npm run test`, and `npm run build` all pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-chuy-n-code-g-p-v-o-th-m-c-src/01-01-SUMMARY.md`
</output>
