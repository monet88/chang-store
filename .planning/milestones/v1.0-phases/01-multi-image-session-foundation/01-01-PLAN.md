---
phase: 01-multi-image-session-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Upscale.tsx
  - components/upscale/UpscaleModeSwitch.tsx
  - components/upscale/UpscaleSessionImageRail.tsx
  - components/upscale/UpscaleQuickPanel.tsx
  - components/upscale/UpscaleStudioStepShell.tsx
  - components/upscale/UpscaleOutputPanel.tsx
  - hooks/useUpscale.ts
  - types.ts
  - locales/en.ts
  - locales/vi.ts
  - __tests__/hooks/useUpscale.test.tsx
  - __tests__/components/Upscale.test.tsx
autonomous: true
requirements: [UPS-01, UPS-03, UPS-04, UPS-05]
user_setup: []
must_haves:
  truths:
    - User can keep multiple uploaded images available in one Upscale session.
    - The newest uploaded image becomes the active session image immediately.
    - User can switch the active session image without losing image-specific Quick Upscale or AI Studio shell state.
    - User can switch between Quick Upscale and AI Studio inside the same Upscale screen without losing in-progress draft state for either lane.
    - AI Studio steps stay visible inside Upscale, with future steps shown disabled until unlocked and earlier changes leaving later outputs visible until rerun.
  artifacts:
    - path: hooks/useUpscale.ts
      provides: Feature-local orchestration for multi-image session state, active-image switching, mode state, and Quick Upscale actions
      min_lines: 80
      exports: [useUpscale]
    - path: components/Upscale.tsx
      provides: Thin screen wiring the hook into the workspace layout
      min_lines: 40
      contains: useUpscale
    - path: components/upscale/UpscaleSessionImageRail.tsx
      provides: Lightweight session image picker with active state and per-image status hints
      min_lines: 30
    - path: components/upscale/UpscaleStudioStepShell.tsx
      provides: Visible AI Studio step header and disabled upcoming-step treatment
      min_lines: 30
    - path: __tests__/hooks/useUpscale.test.tsx
      provides: Regression coverage for multi-image session behavior and mode preservation
      min_lines: 40
  key_links:
    - from: components/Upscale.tsx
      to: hooks/useUpscale.ts
      via: feature hook import and binding
      pattern: useUpscale
    - from: components/Upscale.tsx
      to: components/upscale/UpscaleSessionImageRail.tsx
      via: session rail render path
      pattern: UpscaleSessionImageRail
    - from: components/Upscale.tsx
      to: components/upscale/UpscaleStudioStepShell.tsx
      via: AI Studio shell render path
      pattern: UpscaleStudioStepShell
    - from: hooks/useUpscale.ts
      to: services/imageEditingService.ts
      via: Quick Upscale action
      pattern: upscaleImage
---

<objective>
Build the Phase 1 Upscale foundation as one self-contained vertical slice.

Purpose: establish the multi-image session model, mode coexistence, and internal AI Studio shell before later phases add deeper Gemini analysis and inline prompt workflows.
Output: a thin `Upscale` screen, a dedicated `useUpscale` hook, focused child components for the session workspace, localized UI strings, and regression tests for the new session contract.
</objective>

<execution_context>
@C:/Users/monet/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/monet/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-image-session-foundation/01-CONTEXT.md
@.planning/phases/01-multi-image-session-foundation/01-RESEARCH.md
@.planning/phases/01-multi-image-session-foundation/01-VALIDATION.md

@components/Upscale.tsx
@components/ImageUploader.tsx
@components/ImageComparator.tsx
@components/LookbookOutput.tsx
@components/OutfitAnalysis.tsx
@components/modals/ImageSelectionModal.tsx
@contexts/ApiProviderContext.tsx
@contexts/ImageGalleryContext.tsx
@services/imageEditingService.ts
@types.ts
@locales/en.ts
@locales/vi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract multi-image session orchestration into `useUpscale` and shared types</name>
  <files>hooks/useUpscale.ts, types.ts</files>
  <action>Create the feature hook that owns the Phase 1 runtime contract: session image array, stable active-image identity, Quick Upscale mode state, AI Studio shell step state, loading/error/result handling, and the "newest upload becomes active" rule. Keep provider/model resolution and `upscaleImage` calls inside the hook. Use shared types in `types.ts` only for contracts that must cross component boundaries; keep helper internals private to the hook. Do not introduce a new global store or feature context.</action>
  <verify>npm run test -- useUpscale</verify>
  <done>`useUpscale` exposes a stable state/actions API that covers UPS-01, UPS-04, and UPS-05 without leaking provider logic into UI components.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild `Upscale` into a thin workspace with modular session and AI Studio shell components</name>
  <files>components/Upscale.tsx, components/upscale/UpscaleModeSwitch.tsx, components/upscale/UpscaleSessionImageRail.tsx, components/upscale/UpscaleQuickPanel.tsx, components/upscale/UpscaleStudioStepShell.tsx, components/upscale/UpscaleOutputPanel.tsx, locales/en.ts, locales/vi.ts</files>
  <action>Refactor the current monolithic screen into a coordinator plus focused child components. Add the segmented Quick Upscale / AI Studio switch, a lightweight thumbnail rail with active-image emphasis and light status badges, a visible AI Studio step header with disabled future steps, and one shared sticky output panel. Preserve the existing Quick Upscale path in the same feature screen and wire it through the hook state so switching modes does not reset drafts. Add the needed translation keys in both locale files and keep the mobile layout understandable while preserving the sticky-right-panel desktop intent.</action>
  <verify>npm run build</verify>
  <done>The Upscale UI renders as a two-column workspace where users can upload multiple images, switch the active one, swap between Quick Upscale and AI Studio, and stay inside one screen for step navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for session continuity, active-image selection, and mode preservation</name>
  <files>__tests__/hooks/useUpscale.test.tsx, __tests__/components/Upscale.test.tsx</files>
  <action>Add targeted tests that lock in the Phase 1 contract: newest upload becomes active, multiple images remain available in the session, active-image selection drives the workspace, Quick Upscale and AI Studio drafts survive mode switches, and AI Studio step visibility/unlock rules match the context. Reuse the repo's existing mocking patterns for contexts and image services instead of inventing a new test harness.</action>
  <verify>npm run test -- Upscale</verify>
  <done>Automated tests fail if a later change reintroduces one-image assumptions, destructive mode resets, or hidden/incorrect AI Studio step behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- useUpscale
- [ ] npm run test -- Upscale
- [ ] npm run test
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `components/Upscale.tsx` becomes a thin coordinator rather than the primary logic container
- Phase 1 covers UPS-01, UPS-03, UPS-04, and UPS-05 without implementing Phase 3 or Phase 4 behaviors early
- Desktop keeps a sticky shared output panel and mobile keeps the session rail + step shell understandable
  </success_criteria>

<output>
After completion, create `.planning/phases/01-multi-image-session-foundation/01-01-SUMMARY.md`
</output>
