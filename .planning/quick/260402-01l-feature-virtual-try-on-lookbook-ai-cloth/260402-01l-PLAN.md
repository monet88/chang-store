---
id: 260402-01l
mode: quick
description: "feature Virtual Try-On, Lookbook AI, Clothing Transfer: 1. Thêm batch download, 2. Bỏ block regenerate ảnh khác, 3. Bỏ hardcode max parallel 3 images"
---

# Quick Plan 260402-01l

## Tasks

1. **Fix Virtual Try-On Concurrency and Blocking**
   - **Action**: Replace `BATCH_CONCURRENCY = 3` with `jobs.length` dynamically in `useVirtualTryOn.ts`. Remove global `setIsLoading(true)` from `handleRegenerateSingle` to prevent locking the UI. Add `handleDownloadAll` and UI button in `VirtualTryOn.tsx`.
   - **Files**: `src/hooks/useVirtualTryOn.ts`, `src/components/VirtualTryOn.tsx`

2. **Fix Clothing Transfer Concurrency and Blocking**
   - **Action**: Replace `BATCH_CONCURRENCY = 3` with `jobs.length` dynamically in `useClothingTransfer.ts`. Remove global `setIsLoading(true)` from `handleRegenerateSingle` to prevent locking the UI. Add `handleDownloadAll` and UI button in `ClothingTransfer.tsx`.
   - **Files**: `src/hooks/useClothingTransfer.ts`, `src/components/ClothingTransfer.tsx`

3. **Add Lookbook AI Batch Download**
   - **Action**: Add `handleDownloadAll` to pack main, variations, and closeups into zip in `useLookbookGenerator.ts`. Pass it to `LookbookOutput.tsx` and render the download button.
   - **Files**: `src/hooks/useLookbookGenerator.ts`, `src/components/LookbookGenerator.tsx`, `src/components/LookbookOutput.tsx`

